<h2>GRoups</h2>

<%= link_to "Add a new group", new_group_path %>
<ul>
  <% @groups.each do |group| %>
    <li>
      <%= link_to group.name, group %>
      -
      <%= group.description %>
      <% image_tag group.name %>
    </li>
  <% end %>
</ul>
